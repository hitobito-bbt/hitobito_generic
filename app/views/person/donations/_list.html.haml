= crud_table do |t|
  - t.col(t.sort_header(:description)) { |d| d.description.truncate(50) }
  - t.col(t.sort_header(:issued_at)) { |d| l(d.issued_at) }
  - t.col(t.sort_header(:amount), style: "width: 10%") { |d| content_tag(:span, number_to_currency(d.amount)) }
  - action_col_destroy(t)
